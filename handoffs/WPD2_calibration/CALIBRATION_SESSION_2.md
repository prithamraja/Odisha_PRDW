# Discover calibration session 2 — record

**Date:** 2026-08-15. **Participants:** operator (SME, D20) + PM. **Input:**
package v4 (`v4_depth2/`), the nine new-at-depth-2 findings + the four
session-1 survivors + the depth-default decision. Labels in
`v4_depth2/labeling_sheet.csv` (view1 rows; view2/view3 carry the session-1
baseline per WP-D2c §2.4's slot mapping).

## Results

- **The nine depth-2 findings: all real** (one provisional). Standouts by the
  operator's own reading: #4 (55% of Public Works uncategorised — new
  data-quality subject), #6 (93.9% of general-component spend sits on WORK
  ONGOING), and **#12 — the SME catch of the session**: XV FC tied grants are
  earmarked by rule, so their sanitation+drinking-water share should be 100%
  (or a rule-defined split); the measured 97.6% is neither, making the
  **₹42 lakh of tied money on other focus areas a potential earmark-compliance
  finding**, to be elevated in the report, not footnoted.
- **#10 is real *provisionally***: its 100% share may prove definitional once
  the output_type decode arrives (photo evidence may attach to physical
  outputs by construction). Revisit on decode delivery.
- **#5 stays real with a presentation change → action A8**: the operator wants
  the overspend story as a **ratio**, not an absolute. Mechanism (respects the
  no-materialized-ratios rule): the engine keeps mining the SUM difference;
  the enrichment attaches a **utilization companion** — ratio of sums
  (Σspent / Σplanned) per highlighted group, computed downstream from the
  num/denom columns, like the intensity companion. Lands in WP-D3's scope
  (phase5b enrichment added to its writable set).
- **Depth 2 is now the sample default (operator ratification → D29):** the
  only cost is ~92 minutes per re-mine, accepted. The config default flips in
  WP-D3 (one-line authorized edit) and the canonical candidate set for the
  feed is a fresh default-config re-mine.
- **Framing-over-suppression stands** (operator asked for the explanation;
  satisfied; D28 ruling unchanged). Escape hatch remains: if size artifacts
  crowd the statewide feed, a suppression rule can be added then, on evidence.
- **Still open:** view2 #7's label (PM proposal: spurious; operator has not
  ruled in two sessions — carried once more); #11/#14 labels are PM proposals
  pending confirmation.

## New items produced by this session

1. **A8 utilization companion** (above) → WP-D3.
2. **Report elevation rule for #12**: the tied-grant earmark deviation is a
   compliance-relevant finding; the glossary/framing should state the
   earmark rule so the deviation reads as such. → WP-D3 (phase5b framing).
3. The output_type decode team-ask gains urgency: three real-labeled findings
   (#3, #9, #10) and one provisional hang on it.
